v0.6  a,如果种子库没有运行，则自动启动
      b,如果种子库已经在运行中，新增校验运行中的mysqld进程是否与配置一致。避免脚本部署在一个库，打包的是另外一个库。
      c,打包成tar包的时候，删除掉log目录下的binlog相关日志。以确保无论种子库是否开启binlog，都不会影响gz包的安装。
v0.7  a,发现了脚本合成过程中的bug，未确定原因，因此增加了合成结果的校验，如果合成失败，会报错退出。
      b,打出来的包size过大，鉴于在停库之前已设置了innodb_global_fastshutdown=0，已确保的事务的回滚与提交产生的数据
        修改已刷新到磁盘文件，因此可以删除掉redo日志文件（可以节省几G的未压缩大小）。
      c,修复了log.err缺失的bug；log.err日志文件被删除，导致在安装时候因为log.err不存在导致启动失败。
